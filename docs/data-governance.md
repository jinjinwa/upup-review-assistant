# Data Governance

The admin area includes mock data source governance:

- list synthetic data sources;
- queue a mock sync;
- inspect task status.

The mock worker updates community database rows with synthetic task results so contributors can understand the architecture.
