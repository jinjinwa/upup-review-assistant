# Quickstart

## Requirements

- Docker
- Docker Compose

## Run Locally

```bash
docker compose up --build
```

Open:

```text
http://localhost:8080
```

## Test the Backend

```bash
cd backend
python -m pytest
```

The demo uses only synthetic entries and toy scoring.
